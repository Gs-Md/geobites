services:
  - type: web
    name: geobites
    env: node
    plan: free
    autoDeploy: true

    buildCommand: |
      cd backend
      npm install
      cd ../frontend
      npm install
      npm run build

    startCommand: |
      cd backend
      npm start